name: encontro_de_bandas_website
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.5.0-180.0.dev <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Linter
  very_good_analysis: ^5.1.0

  # State management
  go_router: ^14.1.4

  # Firebase
  firebase_core: ^3.0.0
  firebase_analytics: ^11.0.0

  # Utils
  google_fonts: ^6.2.1
  easy_sidemenu: ^0.6.0
  firebase_crashlytics: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
